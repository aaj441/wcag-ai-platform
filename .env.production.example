# ============================================================================
# WCAG AI Platform - Production Environment Variables
# ============================================================================
# Copy this file and configure for your production environment
# NEVER commit actual secrets to Git!
# ============================================================================

# ============================================================================
# CRITICAL SECURITY (REQUIRED FOR PRODUCTION)
# ============================================================================

# JWT Authentication Secret (REQUIRED)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-generated-secret-here-256-bits

# CORS Allowed Origin (REQUIRED)
# Set to your exact frontend domain (no trailing slash)
CORS_ORIGIN=https://your-frontend-domain.com

# Environment Mode (REQUIRED)
NODE_ENV=production

# ============================================================================
# OPTIONAL SECURITY SETTINGS
# ============================================================================

# API Rate Limiting (requests per 15 minutes per IP)
API_RATE_LIMIT=100

# Webhook Signature Secret (for webhook verification)
WEBHOOK_SECRET=your-webhook-secret-here

# Logging Level (error, warn, info, debug)
LOG_LEVEL=info

# ============================================================================
# DATABASE
# ============================================================================

# PostgreSQL Connection String
DATABASE_URL=postgresql://user:password@host:5432/database?schema=public

# ============================================================================
# EXTERNAL SERVICES
# ============================================================================

# OpenAI API (for AI-powered features)
OPENAI_API_KEY=sk-your-openai-api-key

# Sentry Error Tracking (optional but recommended)
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# Stripe Payment Processing (if using billing features)
STRIPE_SECRET_KEY=sk_live_your-stripe-secret-key
STRIPE_PUBLISHABLE_KEY=pk_live_your-stripe-publishable-key

# SendGrid Email Service (if using email features)
SENDGRID_API_KEY=SG.your-sendgrid-api-key

# Clerk Authentication (if using Clerk)
CLERK_SECRET_KEY=sk_live_your-clerk-secret-key

# ============================================================================
# REDIS / QUEUE (Optional)
# ============================================================================

# Redis URL for queue management
REDIS_URL=redis://default:password@host:6379

# ============================================================================
# APPLICATION SETTINGS
# ============================================================================

# Server Port (Railway auto-assigns, you usually don't need to set this)
PORT=3001

# API Base URL (for webhooks and callbacks)
API_BASE_URL=https://your-api-domain.com

# ============================================================================
# MONITORING & ALERTS
# ============================================================================

# Enable detailed health checks
ENABLE_DETAILED_HEALTH_CHECK=true

# Enable request correlation IDs
ENABLE_CORRELATION_IDS=true

# ============================================================================
# SECURITY CHECKLIST
# ============================================================================
# Before going live, ensure:
# ✅ JWT_SECRET is set (32+ random bytes)
# ✅ CORS_ORIGIN matches your frontend domain exactly
# ✅ NODE_ENV=production
# ✅ DATABASE_URL is set (PostgreSQL)
# ✅ OPENAI_API_KEY is set (if using AI features)
# ✅ All secrets are stored securely (Railway Variables, not in code)
# ✅ Run: npm run test:security to verify
# ============================================================================
